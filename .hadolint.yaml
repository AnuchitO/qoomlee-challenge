ignored:
  # DL3018: pin apk versions — base images here use apk only at the OS layer
  # via official golang/alpine images, no inline apk add of app deps.
  - DL3018

trustedRegistries:
  - docker.io
  - golang
  - alpine

failure-threshold: warning
