apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: qoomlee
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          # TODO: replace with your actual image
          image: qoomlee/payment-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8084
          env:
            - name: PORT
              value: "8084"
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: qoomlee-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: qoomlee-config
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: qoomlee-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: qoomlee-config
                  key: DB_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: qoomlee-secrets
                  key: DB_PASS
            - name: OMISE_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: qoomlee-secrets
                  key: OMISE_PUBLIC_KEY
            - name: OMISE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: qoomlee-secrets
                  key: OMISE_SECRET_KEY
            - name: BOOKING_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: qoomlee-config
                  key: BOOKING_SERVICE_URL
          resources:
            # TODO: tune based on load test results
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8084
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8084
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 2

---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: qoomlee
spec:
  selector:
    app: payment-service
  ports:
    - port: 8084
      targetPort: 8084
