# IMPORTANT: Never commit real secrets.
# Base64-encode your values: echo -n "your_value" | base64
#
# This file is a template — fill in the TODO fields then apply.
# In production, use a secret manager (Vault, AWS Secrets Manager, GCP Secret Manager).
apiVersion: v1
kind: Secret
metadata:
  name: qoomlee-secrets
  namespace: qoomlee
type: Opaque
data:
  # TODO: replace with base64-encoded real values
  # echo -n "qoomlee" | base64  →  cW9vbWxlZQ==
  DB_PASS: cW9vbWxlZQ==               # "qoomlee" — change for production

  # TODO: echo -n "pkey_test_YOUR_KEY" | base64
  OMISE_PUBLIC_KEY: REPLACE_ME

  # TODO: echo -n "skey_test_YOUR_KEY" | base64
  OMISE_SECRET_KEY: REPLACE_ME
